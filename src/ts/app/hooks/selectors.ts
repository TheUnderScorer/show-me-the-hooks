export const hooks = Array.from( document.querySelectorAll( '.smth-hook' ) );
export const hooksToggle = document.querySelector( '#smth_hooks_toggle' ) as HTMLButtonElement;
export const hooksFilterInput = document.querySelector( '#smth_hooks_filter' ) as HTMLInputElement;
